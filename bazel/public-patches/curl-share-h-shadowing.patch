diff --git a/lib/vtls/vtls_scache.c b/lib/vtls/vtls_scache.c
index 9c04b77..07ec65b 100644
--- a/lib/vtls/vtls_scache.c
+++ b/lib/vtls/vtls_scache.c
@@ -47,7 +47,13 @@
 #include "strcase.h"
 #include "url.h"
 #include "llist.h"
-#include "share.h"
+/* "../share.h", not "share.h": this file lives in lib/vtls, so the quoted
+   include falls through to the include path rather than resolving beside the
+   source. The Windows SDK ships its own ucrt/share.h for the _SH_* file-sharing
+   constants, and on this port the SDK include directories precede curl's own,
+   so plain "share.h" picks up the SDK header and every call to Curl_share_lock
+   and CURL_SHARE_ssl_scache below becomes an implicit declaration. */
+#include "../share.h"
 #include "curl_trc.h"
 #include "curl_sha256.h"
 #include "rand.h"
