diff --git a/BUILD.bazel b/BUILD.bazel
index 0fafe5c..b4ae9e8 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -290,14 +290,18 @@ expand_template(
         hdrs = glob(["include/curl/*.h"]),
         copts = select({
             "@platforms//os:windows": [
-                "/DBUILDING_LIBCURL",
-                "/DCURL_DISABLE_LDAP",
-                "/DUSE_IPV6",
-                "/DUSE_SCHANNEL",
-                "/DUSE_WIN32_IDN",
-                "/DUSE_WINDOWS_SSPI",
-                "/DWANT_IDN_PROTOTYPES",
-                "/DWIN32",
+                # These were MSVC /D spellings, which only clang-cl accepts.
+                # This fork targets aarch64-pc-windows-msvc with the ordinary
+                # clang driver, where a leading slash is read as a file path and
+                # every translation unit fails with "no such file or directory".
+                "-DBUILDING_LIBCURL",
+                "-DCURL_DISABLE_LDAP",
+                "-DUSE_IPV6",
+                "-DUSE_SCHANNEL",
+                "-DUSE_WIN32_IDN",
+                "-DUSE_WINDOWS_SSPI",
+                "-DWANT_IDN_PROTOTYPES",
+                "-DWIN32",
             ],
             "@platforms//os:macos": _BASE_CURL_COPTS,
             "//conditions:default": _BASE_CURL_COPTS + [
